# Tooltip

Тултип — базовый компонент, который служит основой для компонента Action Tooltip. Появляется по наведению на кнопки и, как правило, содержит объяснение или дополнительную информацию о действии, которое совершится после клика на кнопку.

Тултип используется только тогда, когда необходимо разместить нестандартный контент внутри подсказки. В остальных случаях рекомендуется использовать Action Tooltip.

### Usage example

```vue
<Tooltip content="Подсказка" position="top" :show-arrow="true" :hover-delay="250">
  <ButtonUi>Наведите на меня</ButtonUi>
</Tooltip>
```

### Usage with spread props

```vue
<template>
  <Tooltip v-bind="tooltipProps">
    <ButtonUi>Наведите</ButtonUi>
  </Tooltip>
</template>

<script setup lang="ts">
const tooltipProps = {
  content: 'Подсказка',
  position: 'top',
  showArrow: true,
  hoverDelay: 250,
  offset: 8
}
</script>
```

### Props

```typescript
content?: string
  // Текст подсказки
  // Если не указан, можно использовать слот #content для кастомного контента
  // Default: ''

position?: 'top' | 'bottom' | 'left' | 'right' | 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right' | 'left-top' | 'left-bottom' | 'right-top' | 'right-bottom'
  // Позиция tooltip относительно триггера
  // Расположение tooltip подбирается так, чтобы tooltip перекрывал минимум контента на странице
  // Оптимальное расположение — там, где перекрытие минимально
  // Default: 'top'

showArrow?: boolean
  // Показывать хвостик (стрелку) указывающий на триггер
  // Default: true

offset?: number
  // Отступ от триггера в пикселях
  // Default: 8

hoverDelay?: number
  // Задержка перед показом (в миллисекундах)
  // Базово используется небольшая задержка появления в 250 мс
  // Если вы предполагаете, что значительная часть пользователей может нажать на кнопку, не читая tooltip, следует использовать задержку
  // Если же предполагается, что, не прочитав подсказку, мало кто станет нажимать на кнопку, то задержку лучше убрать
  // Default: 250

disabled?: boolean
  // Отключить tooltip
  // Default: false
```

### Events

```typescript
@open: () => void
  // Событие открытия tooltip

@close: () => void
  // Событие закрытия tooltip
```

### Slots

```vue
#default
  // Элемент-триггер (обязательный слот)
  // Обычно это кнопка или другой интерактивный элемент

#content
  // Кастомный контент tooltip
  // Если не указан, используется проп content
  // Tooltip может содержать в себе произвольный контент
  // Рекомендуется придерживаться общих гайдлайнов и отступов при проектировании содержимого
```

### Methods

```typescript
open(): void
  // Открыть tooltip программно

close(): void
  // Закрыть tooltip программно

toggle(): void
  // Переключить состояние tooltip

updatePosition(): void
  // Обновить позицию tooltip
```

### Examples

#### Basic usage

```vue
<Tooltip content="Подсказка">
  <ButtonUi>Наведите на меня</ButtonUi>
</Tooltip>
```

#### Different positions

```vue
<!-- Сверху -->
<Tooltip content="Подсказка сверху" position="top" :show-arrow="true">
  <ButtonUi>Top</ButtonUi>
</Tooltip>

<!-- Снизу -->
<Tooltip content="Подсказка снизу" position="bottom" :show-arrow="true">
  <ButtonUi>Bottom</ButtonUi>
</Tooltip>

<!-- Слева -->
<Tooltip content="Подсказка слева" position="left" :show-arrow="true">
  <ButtonUi>Left</ButtonUi>
</Tooltip>

<!-- Справа -->
<Tooltip content="Подсказка справа" position="right" :show-arrow="true">
  <ButtonUi>Right</ButtonUi>
</Tooltip>
```

#### With custom content

```vue
<Tooltip position="bottom" :show-arrow="true">
  <ButtonUi>Кастомный контент</ButtonUi>
  <template #content>
    <div>
      <div style="font-weight: 600; margin-bottom: 0.25rem;">Заголовок</div>
      <div style="font-size: 0.875rem;">Это tooltip с произвольным контентом</div>
    </div>
  </template>
</Tooltip>
```

#### With large content

```vue
<Tooltip position="bottom" :show-arrow="true">
  <ButtonUi>Большой контент</ButtonUi>
  <template #content>
    <div style="max-width: 200px;">
      <div style="font-weight: 600; margin-bottom: 0.5rem;">Подробная информация</div>
      <div style="font-size: 0.875rem; line-height: 1.5;">
        Это tooltip с большим объемом содержимого. Здесь может быть подробное описание функционала кнопки или дополнительная информация.
      </div>
    </div>
  </template>
</Tooltip>
```

#### With delay

```vue
<!-- С задержкой (рекомендуется для кнопок, которые часто нажимают) -->
<Tooltip content="Подсказка" :hover-delay="500">
  <ButtonUi>С задержкой</ButtonUi>
</Tooltip>

<!-- Без задержки (для важных подсказок) -->
<Tooltip content="Подсказка" :hover-delay="0">
  <ButtonUi>Без задержки</ButtonUi>
</Tooltip>
```

#### Without arrow

```vue
<Tooltip content="Подсказка без хвостика" :show-arrow="false">
  <ButtonUi>Без хвостика</ButtonUi>
</Tooltip>
```

#### On different elements

```vue
<!-- На кнопке -->
<Tooltip content="Сохранить изменения" position="top">
  <ButtonUi icon="💾" icon-position="only" />
</Tooltip>

<!-- На ссылке -->
<Tooltip content="Перейти на страницу" position="bottom">
  <a href="#">Ссылка</a>
</Tooltip>

<!-- На иконке -->
<Tooltip content="Дополнительная информация" position="right">
  <span style="font-size: 1.5rem; cursor: pointer;">ℹ️</span>
</Tooltip>
```

#### Usage examples

```vue
<template>
  <div>
    <!-- Кнопки с подсказками -->
    <div style="display: flex; gap: 1rem;">
      <Tooltip content="Сохранить изменения" position="top" :show-arrow="true">
        <ButtonUi icon="💾" icon-position="only" />
      </Tooltip>
      
      <Tooltip content="Удалить элемент" position="top" :show-arrow="true">
        <ButtonUi icon="🗑️" icon-position="only" />
      </Tooltip>
      
      <Tooltip content="Редактировать" position="top" :show-arrow="true">
        <ButtonUi icon="✏️" icon-position="only" />
      </Tooltip>
    </div>
    
    <!-- Кнопки с разными задержками -->
    <div style="display: flex; gap: 1rem; margin-top: 1rem;">
      <!-- Без задержки для важных подсказок -->
      <Tooltip content="Копировать в буфер обмена" position="bottom" :hover-delay="0">
        <ButtonUi>Копировать</ButtonUi>
      </Tooltip>
      
      <!-- С задержкой для кнопок, которые часто нажимают -->
      <Tooltip content="Это действие нельзя отменить" position="bottom" :hover-delay="500">
        <ButtonUi variant="danger">Удалить</ButtonUi>
      </Tooltip>
    </div>
  </div>
</template>
```

#### With spread props

```vue
<template>
  <Tooltip v-bind="tooltipConfig">
    <ButtonUi>Наведите</ButtonUi>
  </Tooltip>
</template>

<script setup lang="ts">
const tooltipConfig = {
  content: 'Подсказка',
  position: 'top',
  showArrow: true,
  hoverDelay: 250,
  offset: 8
}
</script>
```

### Размеры и стиль

Размер tooltip адаптируется под контент. Цвет и стиль tooltip — неизменный.

### Контент внутри

Tooltip может содержать в себе произвольный контент. Рекомендуется придерживаться общих гайдлайнов и отступов при проектировании содержимого tooltip.

### Тайминг

В зависимости от цели, которую выполняет tooltip, можно менять тайминг его появления и исчезновения. Базово используется небольшая задержка появления в 250 мс, а исчезает tooltip мгновенно, после того как пользователь убирает курсор с кнопки.

- **Если вы предполагаете, что значительная часть пользователей может нажать на кнопку, не читая tooltip**, следует использовать задержку, чтобы не раздражать пользователей ненужными подсказками.
- **Если же предполагается, что, не прочитав подсказку, мало кто станет нажимать на кнопку**, то задержку лучше убрать, чтобы не заставлять пользователя ждать описания функционала.

### Положение

Расположение tooltip подбирается так, чтобы tooltip перекрывал минимум контента на странице. Оптимальное расположение — там, где перекрытие минимально. Компонент автоматически корректирует позицию, если tooltip выходит за границы экрана.