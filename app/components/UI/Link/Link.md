# Link Component

Компонент для создания ссылок с различными стилями и вариантами.

## Использование

### Базовое использование

```vue
<Link href="https://example.com">Перейти на сайт</Link>
```

### С Vue Router

```vue
<Link to="/about">О нас</Link>
<Link :to="{ name: 'profile', params: { id: 1 } }">Профиль</Link>
```

### Варианты стилей

```vue
<Link href="#" variant="primary">Основная ссылка</Link>
<Link href="#" variant="secondary">Вторичная ссылка</Link>
<Link href="#" variant="muted">Приглушенная ссылка</Link>
<Link href="#" variant="success">Успешное действие</Link>
<Link href="#" variant="danger">Опасное действие</Link>
<Link href="#" variant="warning">Предупреждение</Link>
<Link href="#" variant="info">Информация</Link>
```

### Размеры

```vue
<Link href="#" size="xs">Очень маленькая</Link>
<Link href="#" size="sm">Маленькая</Link>
<Link href="#" size="md">Средняя (по умолчанию)</Link>
<Link href="#" size="lg">Большая</Link>
<Link href="#" size="xl">Очень большая</Link>
```

### Подчеркивание

```vue
<!-- С подчеркиванием (по умолчанию) -->
<Link href="#" :underline="true">С подчеркиванием</Link>

<!-- Без подчеркивания (появляется при hover) -->
<Link href="#" :underline="false">Без подчеркивания</Link>
```

### С иконками

```vue
<!-- Иконка слева -->
<Link href="#" icon="→" icon-position="left">Перейти</Link>

<!-- Иконка справа -->
<Link href="#" icon="→" icon-position="right">Перейти</Link>

<!-- Только иконка -->
<Link href="#" icon="🔗" icon-position="only" />
```

### Внешние ссылки

```vue
<Link href="https://example.com" target="_blank">
  Открыть в новой вкладке
</Link>
```

### Отключенное состояние

```vue
<Link href="#" :disabled="true">Отключенная ссылка</Link>
```

### Обработка кликов

```vue
<Link href="#" @click="handleClick">Кликни меня</Link>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `href` | `string` | - | URL для обычной ссылки |
| `to` | `RouteLocationRaw` | - | Путь для Vue Router |
| `variant` | `'primary' \| 'secondary' \| 'muted' \| 'success' \| 'danger' \| 'warning' \| 'info'` | `'primary'` | Вариант стиля ссылки |
| `size` | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl'` | `'md'` | Размер ссылки |
| `disabled` | `boolean` | `false` | Отключить ссылку |
| `underline` | `boolean` | `true` | Показывать подчеркивание |
| `icon` | `string` | - | Иконка (emoji или символ) |
| `iconPosition` | `'left' \| 'right' \| 'only'` | `'left'` | Позиция иконки |
| `target` | `'_blank' \| '_self' \| '_parent' \| '_top'` | - | Атрибут target для ссылки |
| `rel` | `string` | - | Атрибут rel (автоматически добавляется для `target="_blank"`) |

## Events

| Event | Payload | Description |
|-------|---------|-------------|
| `click` | `MouseEvent` | Вызывается при клике на ссылку |

## Примечания

- Если указан `to`, компонент использует `router-link`, иначе обычный `<a>` тег
- Для внешних ссылок с `target="_blank"` автоматически добавляется `rel="noopener noreferrer"`
- Отключенные ссылки не реагируют на клики и имеют пониженную непрозрачность
