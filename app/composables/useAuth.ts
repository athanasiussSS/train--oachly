// Composable для работы с авторизацией и регистрацией

export const useAuth = () => {
  // ========== АВТОРИЗАЦИЯ ==========
  // login(credentials) - вход пользователя
  //   - Принимает: emailOrPhone, password
  //   - Отправляет запрос на бекенд
  //   - Бекенд проверяет: есть ли пользователь, верный ли пароль, подтверждён ли email, не заблокирован ли
  //   - Возвращает: { token, user: { id, name, hasTeacherProfile } }
  //   - Сохраняет токен и данные в store
  //   - Возвращает данные пользователя
  
  // ========== РЕГИСТРАЦИЯ ==========
  // sendVerificationCode(emailOrPhone) - отправка кода подтверждения
  //   - Отправляет код на email/SMS
  //   - Возвращает успех/ошибку
  
  // verifyCode(emailOrPhone, code) - проверка кода
  //   - Проверяет код подтверждения
  //   - Возвращает успех/ошибку
  
  // register(data) - создание аккаунта
  //   - Принимает: emailOrPhone, password, name, verificationCode, agreeToTerms
  //   - Отправляет запрос на бекенд
  //   - Бекенд создаёт аккаунт (роль: Ученик)
  //   - Email помечается как подтверждённый
  //   - Возвращает: { token, user }
  //   - Сохраняет токен и данные в store
  
  // resendCode(emailOrPhone) - повторная отправка кода
  //   - Отправляет код повторно
  //   - Запускает таймер (5 минут)
  
  // ========== ВЫХОД ==========
  // logout() - выход пользователя
  //   - Очищает токен и данные из store
  //   - Редирект на /login
  
  // ========== ПРОВЕРКА ==========
  // isAuthenticated() - проверка авторизации
  //   - Проверяет наличие токена в store
  //   - Возвращает boolean
  
  // getCurrentUser() - получение текущего пользователя
  //   - Возвращает данные из store
  
  return {
    // Методы и состояние
  }
}
